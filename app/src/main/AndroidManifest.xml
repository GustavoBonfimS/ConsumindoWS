<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.consumindows">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="WIG"
        android:roundIcon="@mipmap/ic_launcher_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaPesquisa" />
        <activity android:name=".telaBuscar" />
        <activity
            android:name=".Index"
            android:label="@string/title_activity_index"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastrocomLayout" />
        <activity android:name=".telaEmpresa" />
        <activity android:name=".testeIntente" />
        <activity android:name=".CadastrarUsuario" />
        <activity android:name=".TesteAvaliacao" />
        <activity android:name=".loginActivity">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>